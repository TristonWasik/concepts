import { Result } from "../utils/types";

/**
 * Returns a status message based on the result.
 * @param result - The result object containing status information.
 * @returns A string message representing the status.
 */
export function getStatusMessage(result: Result): string {
  return "";
}
