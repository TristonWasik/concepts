/**
 * Wraps a value or an array of values in an array.
 * If the input is already an array, it returns it as is.
 * @param input - A value or an array of values.
 * @returns
 */
export function wrapInArray<T>(input: T | T[]): T[] {
  return [];
}
